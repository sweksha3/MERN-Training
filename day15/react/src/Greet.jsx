function Greet(props){
    return(
        <>
        <h2>Good Morning {props.name}</h2>       
      {/* props is the argument that we would pass we can name it anything like abc,xyz,etc  and name is 
      the variable which is used  is used to store the values*/}
        </>
    )
}

export default Greet